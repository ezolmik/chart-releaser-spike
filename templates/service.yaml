apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-uploader-spike.fullname" . }}
  labels:
    {{- include "chart-uploader-spike.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-uploader-spike.selectorLabels" . | nindent 4 }}
